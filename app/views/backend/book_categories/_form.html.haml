= form_for [:backend, @category], html: { class: "backend-little-form" } do |f|
  .form-wrapper
    - if @category.errors.any?
      = render "common/errors", object: @category

    .field
      = f.label :name, "Наименование категории :"
      = f.text_field :name
      %br
    .field
      = f.label :description, "Описание категории :"
      = f.text_area :description
      %br
      %span.input-helper Этот заголовок, будет помещён в описание книги
      %br
    .actions
      = f.submit 'Сохранить', :class => "button-primary-light-green"